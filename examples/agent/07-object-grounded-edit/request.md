Open the public reaction figure `figure1.cdxml`.

Find molecule object `object:obj_mol_004`, inspect the local region around it,
and change node `node:1176604361` from `Me` to `OMe`.

The edit must be scoped to `object:obj_mol_004`, must not create or delete
objects, and must prove through a document diff that no unexpected selectors
changed.
